# Copy thành .env.tunnel.local (đã gitignore) — KHÔNG commit file có mật khẩu.

SSH_HOST=14.232.210.125
SSH_PORT=2222
SSH_USER=provision
# Cách 1: mật khẩu (cần sshpass)
SSH_PASSWORD=
# Cách 2: key (ưu tiên hơn password nếu điền)
# SSH_IDENTITY_FILE=/Users/you/.ssh/id_ed25519

# Host port trên server (khớp deploy/remote/docker-compose.giapha-infra.yml)
REMOTE_PG_PORT=15432
REMOTE_REDIS_PORT=16379
REMOTE_MINIO_PORT=19000
REMOTE_MINIO_CONSOLE_PORT=19001
REMOTE_ES_PORT=19200
REMOTE_KC_PORT=18086
