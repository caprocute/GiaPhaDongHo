/**
 * Module sự kiện dòng họ — public {@code api/} + {@code events/} (F6 / R2.3).
 */
@org.springframework.modulith.ApplicationModule(
    displayName = "event",
    allowedDependencies = { "core :: security", "core :: lunar" }
)
package vn.giapha.event;
