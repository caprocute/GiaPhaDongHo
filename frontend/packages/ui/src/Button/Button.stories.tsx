import { Button } from "./Button";

export default { title: "Button", component: Button };

export const Primary = { args: { children: "Đăng nhập", variant: "primary" } };
export const Secondary = { args: { children: "Hủy", variant: "secondary" } };
