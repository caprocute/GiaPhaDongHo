# ClanSeal — Ấn tổ dòng họ

> **Done 4 mảnh (TK-04):** ① Spec · ② Code · ③ Story · ④ Usage.

Ấn triện dùng chung portal masthead (`PublicHeader`) và sidebar CRM admin.

```tsx
import { ClanSeal } from "@giapha/ui";

<ClanSeal />
<ClanSeal compact />
```

Chỉ dùng design token (`--color-heritage-*`); không hardcode màu.
